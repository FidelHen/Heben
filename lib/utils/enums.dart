enum UserGoal { strong, healthy, lean, motivated }

enum UserRole { user, friend }

enum CurrentPostPopularity { trending, featured, pinned, normal }

enum DurationChallenge { oneDay, oneWeek, oneMonth }

enum TypeOfList { all, likes, bookmarks }

enum PostContentType { text, image, video, challenge }

enum ImageOption { profile, background }
